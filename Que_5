pipeline {
 agent any 
  environment {
	  	name='Brad'
	}
   stages ('All details') {
      stage ('JOb details') {
        steps {
           echo "Hi my name is ${name}. The job name is ${env.JOB_NAME} with the job url ${env.JOB_URL} and the build number is ${env.BUILD_NUMBER}"
           emailext body: '''Hello Brad,
           PFA the console output of the Job Que 5''', subject: 'Job details', to: 'Bhavani.shankarn1995@gmail.com'
        }

      }
   }
}
