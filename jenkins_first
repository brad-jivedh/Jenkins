pipeline {
  agent any
    stages (creating an file simple) {
      stage (1-stage) {
       steps {
        sh 'touch pipe'
    }
    }
       stage (2-stage) {
         steps {
          sh 'touch pipe2'
    }
   }
}
}
